apiVersion: v1
kind: Service
metadata:
  name: {{ template "hygieia-api-audit.fullname" . }}
  labels:
    app: {{ template "hygieia-api-audit.name" . }}
    chart: {{ template "hygieia-api-audit.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hygieia-api-audit.name" . }}
    release: {{ .Release.Name }}
