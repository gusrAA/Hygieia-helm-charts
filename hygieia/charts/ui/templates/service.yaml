apiVersion: v1
kind: Service
metadata:
  name: {{ template "hygieia-ui.fullname" . }}
  labels:
    app: {{ template "hygieia-ui.name" . }}
    chart: {{ template "hygieia-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hygieia-ui.name" . }}
    release: {{ .Release.Name }}
